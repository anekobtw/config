# nvim-config
I am using nvim in [Windows Terminal](https://github.com/microsoft/terminal). The font I love and always use is [Maple Mono](https://github.com/subframe7536/maple-font). It can be installed via:

```
scoop bucket add nerd-fonts
scoop install Maple-Mono-NF
```

